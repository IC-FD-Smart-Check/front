# URL base da API, COM o /api no final e SEM barra no fim.
# Obrigatoria em producao — sem ela o app aponta para http://localhost:8080/api.
# Na Vercel: Settings > Environment Variables.
VITE_API_URL=https://api.seu-dominio.com/api
